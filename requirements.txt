# Runtime dependencies for the TobiiEyeTracker application.
# NOTE: the UI uses PySide6 (not PyQt5). Install into the project venv.
# Audio uses the Windows-native winsound (stdlib), so no audio package is required.
numpy==2.2.6
opencv-python==4.12.0.88
PySide6==6.11.1
pyqtgraph==0.13.7
tobii-research==2.1.0

# Optional - only needed by core/bbox_utils.py (YOLO/torch path), not the core app:
# torch
